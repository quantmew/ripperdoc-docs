---
title: 安装
description: 在你的系统上安装 Ripperdoc
---

## 环境要求

- Python 3.10 或更高版本
- pip 或 uv 包管理器

## 安装方式

### 使用 pip（推荐）

```bash
pip install ripperdoc
```

### 使用 uv

```bash
uvx tool install ripperdoc
```

### 从源码安装

```bash
git clone https://github.com/quantmew/ripperdoc.git
cd ripperdoc
pip install -e .
```

## 验证安装

```bash
ripperdoc --version
```

你应该能看到版本号。然后运行：

```bash
ripperdoc
```

这会启动一个交互式会话。首次启动会引导你设置 LLM 端点，之后输入消息即可开始与 AI 对话。

## 下一步

- [快速开始](/zh-CN/docs/quickstart) - 立即开始使用 Ripperdoc
- [配置](/zh-CN/docs/configuration) - 自定义 Ripperdoc 设置
